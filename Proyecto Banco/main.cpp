/**
 * @brief Función principal que inicializa y ejecuta 
 * la aplicación del banco.
 * @param banco Objeto Banco para gestionar la lógica 
 * del sistema bancario.
 */
#include "Banco.h"
int main() {
    system("chcp 65001 > nul");
    Banco banco;
    banco.ejecutar();
    return 0;
}
#include "Banco.h"
int main() {
    system("chcp 65001 > nul");
    Banco banco;
    banco.ejecutar();
    return 0;
}